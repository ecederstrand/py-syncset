language: python
os: linux
dist: bionic
sudo: true
python:
- '3.5'
- '3.6'
- '3.7'
- '3.8'
- nightly
# command to install dependencies
install:
- python setup.py -q install
- pip install coverage
- pip install coveralls
# command to run tests
script:
- coverage run --source=syncset setup.py test
after_success:
    coveralls
